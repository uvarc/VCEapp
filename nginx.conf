server {
  location @flask {
    include uwsgi_params;
    uwsgi_pass unix://tmp/uwsgi.sock;
  }
  location / {
    try_files $uri @flask;
  }
  location /Videos {
    autoindex on;
    autoindex_exact_size off;
    root  /project/DSone/jaj4zcf;
  } 

}
