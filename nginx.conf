user root;

server {
  location @flask {
    include uwsgi_params;
    uwsgi_pass unix://tmp/uwsgi.sock;
  }
  
  location / {
    try_files $uri @flask;
  }
  
  location /static {
       alias /var/www;
   }

   gzip on;
   gzip_types image/* text/css;
   gunzip on;
}
